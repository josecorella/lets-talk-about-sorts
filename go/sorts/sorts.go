package sorts

import "fmt"

func SelectionSort(list []int) {
    for i := 0; i < len(list); i++ {
        for j := i + 1; j < len(list); j++ {
            if 
        }
    }
}


func PrintArray(sort_name string, list []int)  {
    fmt.Printf("%s: ", sort_name)
    for v := range list {
        fmt.Printf("%d ", list[v])
    }
    fmt.Println()
}
